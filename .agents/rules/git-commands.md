# Git Command Guidelines

At the end of every response where file modifications or creations take place:
1. Do NOT execute `git add` or `git commit` commands automatically.
2. Provide the explicit `git add <file_path>` commands in a copyable code block at the end of the response for each changed file.
